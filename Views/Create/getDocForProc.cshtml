@model docZapr
@inject getList list
@foreach(var d in await list.getDocForProc(Model.regId))
{
   <label style="display: block;"><input type="checkbox" value="@d.Key" id="@d.Key" name="NameDocDop" checked="@(Model.selectList!=null?Model.selectList.Contains((Guid)d.Key):false)"/> @d.Value</label>
}